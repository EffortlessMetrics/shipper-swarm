# Scheduler conformance ledger for 0.5.0

Status: Evidence reconciled; pending final exact-head closeout
Owner: Shipper swarm maintainers
Milestone: 0.5.0 release blockers
Linked issue: [#153](https://github.com/EffortlessMetrics/shipper-swarm/issues/153)
Source seam: `crates/shipper-core/src/engine/parallel/tests.rs`

This ledger is the acceptance map for the final #153 conformance closeout. It
records current evidence on `origin/main` and separates existing focused proof
from the cross-scheduler comparisons still required. Timestamps, temporary
paths, and other genuinely nondeterministic values may be normalized; semantic
state, events, receipts, invocation counts, and diagnostics may not.

## Contract

For each scenario that has a sequential analogue, run the same fixture through
the sequential and parallel schedulers and compare. Event comparison is a
partial-order check: preserve lifecycle order within each package, enforce
dependency and scheduler-level ordering, and treat events from independent
packages as interchangeable when their interleavings differ validly.

| Evidence | Required comparison |
| --- | --- |
| Package result | status, success/failure, and error class |
| State | terminal state, attempts, and prior progress |
| Events | semantic event partial order, including per-package lifecycle and dependency/level ordering |
| Rebuild | event-to-state projection for every claimed reconstructible field |
| Receipt | status, reason, execution result, and evidence |
| Cargo | invocation count and no duplicate upload |
| Webhooks | run/package event counts and failure contract |
| Reporting | buffered messages reach the caller |

Worker panic/join failure has no sequential analogue. It is therefore a
parallel-only contract: the caller receives a run-level error, synchronized
state is durable, and buffered reporter output is replayed before return.

## Acceptance ledger

| Scenario required by #153 | Existing evidence on current main | Runs both schedulers? | Gap / required closeout |
| --- | --- | :---: | --- |
| Already published | `mode_parity_corpus_sequential_matches_parallel` / `AlreadyPublishedInState`; `test_publish_package_skips_already_published` | Yes for corpus | Covered: terminal state, normalized receipt parity, semantic event parity, rebuild parity, and zero registry calls. |
| Clean publish | `mode_parity_corpus_sequential_matches_parallel` / `CleanPublish`; `test_publish_package_publishes_successfully` | Yes for corpus | Covered: normalized receipt/evidence parity, semantic lifecycle-event parity, rebuild parity, and successful terminal state. |
| Retryable failure and exhaustion | `mode_parity_corpus_sequential_matches_parallel` / `RetryableExhaustion`; `test_publish_package_retries_on_transient`; `publish_package_retry_exhaustion_preserves_retryable_class`; `test_run_publish_mode_retry_parity_for_multi_level_plan` | Yes for corpus | Covered: retryable terminal class, attempts, semantic events, normalized receipts, rebuild parity, and exactly two Cargo attempts per mode. |
| Permanent failure | `mode_parity_corpus_sequential_matches_parallel` / `PermanentFailure`; `test_publish_package_handles_permanent_failure` | Yes for corpus | Covered: terminal error class, normalized receipt/event parity, rebuild parity, and caller-visible failure. |
| Ambiguous result -> visible | `mode_parity_corpus_sequential_matches_parallel` / `AmbiguousResolvesPublished`; `reconcile_bdd_ambiguous_resolves_to_published` | Yes for corpus | Covered: both-mode reconciliation event, published result, normalized evidence parity, and exactly one retry after the ambiguous upload. |
| Ambiguous result -> not published and safe retry | `mode_parity_corpus_sequential_matches_parallel` / `AmbiguousRetriesWhenNotPublished`; `reconcile_bdd_ambiguous_resolves_to_not_published_then_retries`; `publish_package_final_chance_success_emits_package_published_event` | Yes for corpus | Covered: both-mode NotPublished reconciliation, exactly one safe retry, terminal ambiguous failure, semantic event parity, and Cargo count. |
| Ambiguous result -> StillUnknown | `mode_parity_corpus_sequential_matches_parallel` / `AmbiguousStillUnknown`; `reconcile_bdd_ambiguous_resolves_to_still_unknown` | Yes for corpus | Covered: fresh ambiguous-upload StillUnknown in both modes, durable reconciliation event, Ambiguous state, and no blind retry. |
| Upload -> readiness timeout -> later visibility | `mode_parity_corpus_sequential_matches_parallel` / `ReadinessTimeoutThenVisible` | Yes for corpus | Covered: readiness timeout/complete/published event order, one upload, normalized receipt evidence parity, and rebuilt terminal state. |
| Interruption and resume | Deterministic: `crates/shipper-cli/tests/bdd_resume.rs::given_interrupted_state_when_resume_then_continues_where_left_off`, `crates/shipper-cli/tests/e2e_resume.rs::resume_after_partial_publish`, and `crates/shipper-cli/tests/e2e_rehearse.rs::rehearsal_interrupted_publish_then_resume_preserves_invariants`. Hosted ignored rehearsal: `live_runner_interruption_seed_uploads_shipper_artifact` and `live_runner_interruption_resume_downloaded_artifact_preserves_invariants` in `crates/shipper-cli/tests/e2e_rehearse.rs`. | No single core corpus | Keep deterministic tests in the non-ignored proof gate. Retain the two ignored hosted tests as release evidence and distinguish their runner/workflow dependency. |
| `--resume-from` skip | `test_resume_from_skips_earlier_levels`; `test_resume_from_skip_mode_parity_preserves_durable_evidence`; `test_resume_from_nonexistent_skips_all_levels` | Yes for dedicated parity fixture | Covered: sequential/parallel receipt and semantic event comparison for pre-resume packages, preserved prior attempts, rebuild parity, and no fabricated timestamps. |
| Partial multi-package failure | `test_partial_multi_package_failure_mode_parity`; `test_partial_success_within_level`; `test_execution_result_partial_failure`; `test_independent_failures_both_reported` | Yes for dedicated parity fixture | Covered: package results, terminal state/attempts, package-local event order, rebuild, receipt absence on failure, Cargo count, caller-visible error, and reporter replay. |
| Parallel worker/join failure | `parallel_worker_join_failure_synchronizes_state_and_replays_reporter` | Parallel-only | Covered by an injected join failure: the caller receives a run-level error, completed worker state and events remain synchronized/durable, and buffered reporter output is replayed before return. |
| Webhook delivery failure | `test_webhook_failure_is_non_blocking_and_mode_parity_holds`; `test_public_publish_webhook_counts_match_between_modes`; `test_webhook_events_sent_on_publish` | Yes for selected parity tests | Covered: non-blocking failure behavior, exactly one run-start/package-success/run-complete event count per mode, and delivery count/signature parity. |
| Event-write failure | `mode_parity_corpus_sequential_matches_parallel` / `EventWriteFailure` | Yes for corpus | Covered: fail-before-Cargo assertions, stable sequential/parallel error contract, pending state, no fabricated attempts, and semantic evidence parity. |
| State-write failure | `mode_parity_corpus_sequential_matches_parallel` / `StateWriteFailure`; `test_state_persisted_to_disk_after_level` | Yes for corpus | Covered: event-first behavior, stable projection error, no receipt, pending in-memory state, terminal event rebuild, and semantic parity. |
| Event-to-state rebuild equivalence | `assert_mode_parity_rebuild` in `mode_parity_corpus_sequential_matches_parallel`; dedicated resume/partial/state-write/worker tests | Yes for all accepted core cases | Covered for every corpus case and the parallel-only worker contract; interruption/resume remains additionally covered by CLI tests and hosted rehearsal evidence. |

## Closeout requirements

The implementation PR may close #153 only when:

1. Every row above is linked to an exact test or explicitly marked as the
   parallel-only worker failure contract.
2. The shared executor remains the only package-execution authority.
3. The shared corpus compares semantic events, rebuilt state, receipts, Cargo
   calls, webhooks, and reporter output where the scenario applies.
4. The issue body is updated to remove obsolete duplicate-authority paths and
   links this ledger plus the exact-head test evidence.
5. The complete routed Rust gate and the focused conformance command pass on
   the final PR head.

## Proof commands

```text
cargo fmt --all -- --check
cargo nextest run -p shipper-core --lib --locked --profile ci
cargo nextest run -p shipper-cli --test bdd_parallel --locked --profile ci
cargo nextest run -p shipper-cli --test e2e_rehearse --locked --profile ci
cargo xtask policy-report
git diff --check
```

The ignored live interruption rehearsal is a separate release-evidence gate;
local execution does not replace its hosted self-hosted-runner check.

## Non-goals

- No new public crate or public executor type.
- No broad module-topology cleanup.
- No release publication, tag, credential, or release-authority mutation.
- No normalization that hides a semantic sequential/parallel disagreement.
