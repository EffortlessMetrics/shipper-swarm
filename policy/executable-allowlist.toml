# Executable file allowlist.
#
# Any tracked file with the git executable bit must be receipted here with the
# reason it is executable. No file should become executable without a policy
# receipt.
#
# This file ships empty in PR 2. PR 3 receipts any current executables.

schema_version = "1.0"
policy = "executable-allowlist"
owner = "EffortlessMetrics"
status = "active"

[[file]]
path = "scripts/ci/no-bare-self-hosted.sh"
kind = "ci_policy_script"
surface = "ci"
classification = "executable"
owner = "release/ci"
reason = "Executable runner-policy guard invoked by runner-routing-guard.yml."
covered_by = [".github/workflows/runner-routing-guard.yml"]
created = "2026-07-12"
review_after = "2026-10-12"
