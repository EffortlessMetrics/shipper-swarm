# 0.5.0 Release-Candidate Readiness

Status: candidate preparation in `shipper-swarm`; not published

The August candidate and runs below are **historical evidence, superseded for
publication identity**. The [preparation control record](0.5.0-preparation.md)
and [swarm #255](https://github.com/EffortlessMetrics/shipper-swarm/issues/255)
own the current unfrozen candidate. This retained record does not establish
that earlier artifacts remain available or that current source passes those
checks.

The swarm repository is the development and proof surface. The separate
`EffortlessMetrics/shipper` repository remains the release authority for the
0.5.0 tag, crates.io publication, GitHub Release, signing, and release
credentials. This document records claims to carry into that authority; it is
not publication authorization.

## Stabilized implementation

- Sequential and parallel schedulers delegate package behavior to one internal
  execution authority.
- Durable transitions are event-first: `events.jsonl` is truth, `state.json` is
  a projection, and `receipt.json` is a derived summary.
- Readiness polling has one shared kernel with attempt, backoff, actual-sleep,
  timeout, cancellation, and visibility evidence.
- Resume skips, run-start notifications, ambiguous outcomes, and worker/join
  failures have explicit evidence contracts.
- Registry destinations, redirects, private-network opt-in, and credential
  authority are validated at the network boundary.
- Authorization redaction is context-aware; webhook secrets are excluded from
  execution evidence and diagnostic surfaces.
- New encryption writes use KDF v2 / 600,000 PBKDF2-HMAC-SHA256 iterations;
  unversioned KDF v1 artifacts remain decryptable at 100,000 iterations.
- Blank or whitespace-only OIDC request variables are unavailable and are
  diagnosed without exposing values.

### Webhook-secret data-flow audit

`WebhookConfig` remains serializable because the configuration crate uses
serde/TOML round-tripping for user configuration. That serialization boundary
is not used to create `events.jsonl`, `state.json`, `receipt.json`, structured
command output, or diagnostic messages: those surfaces serialize their own
event, state, receipt, report, and finding types and do not contain
`WebhookConfig` or `RuntimeOptions`.

The runtime configuration type has a manual `Debug` implementation that
redacts both the webhook URL and signing secret. The webhook crate's secret-
bearing snapshot now exercises that redacted diagnostic path, and its fixture
value is assembled at runtime rather than stored as a credential-like literal.
Configuration serialization remains covered by the explicit configuration
round-trip tests; diagnostic snapshots do not serialize the live secret.

## Compatibility guarantees

0.5 artifacts are rebuildable for every field claimed by the event vocabulary.
0.4 events, state, and receipts remain readable and safely resumable; fields
unknown to the 0.4 vocabulary remain unknown rather than fabricated.

## Required exact-SHA evidence

The final candidate bundle must be tied to one immutable swarm SHA and include:

- routed Rust formatting, Clippy, nextest, docs, policy, and package-surface
  proof;
- `shipper-core` conformance plus CLI parallel and interruption/resume tests;
- 0.4 fixture load/resume and KDF v1/v2 compatibility;
- webhook and authorization non-disclosure matrix;
- registry destination, resolved-address, redirect, and credential-authority
  matrix;
- topological package dry-run, facade install smoke, version/help smoke, and
  semver reports against 0.4.0;
- release-workflow target-platform, publication-order, and interrupted-train
  resume evidence.
- the release workflow's binary matrix exercised with the non-publishing
  `mode=binaries` dispatch on the approved release-authority SHA, with retained
  artifacts available for the intended release window.

## Historical frozen candidate and promotion evidence

Captured on 2026-08-03, the earlier frozen swarm candidate was
`488734f2cfd61a82b8e5570ee9ce822546be4726`. It includes the final
webhook-secret diagnostic fix from `e5088e33ce0f87a93790266add0f5180b29ce4a`,
the isolated package-verification gate from PR #241, and the real 0.4.0
resume-compatibility fix from PR #239. The candidate was promoted to the
release authority with non-fast-forward merge commit
`978d0cd6b598f8967ac7f9adf358192362762e9c`. This promotion record is not
publication authorization.

| Evidence | Result |
| --- | --- |
| [main CI run](https://github.com/EffortlessMetrics/shipper-swarm/actions/runs/30841367750) | success for every fan-out job, including nextest, docs, policy, security, install, BDD, fuzz, release build, target, MSRV, and crypto checks |
| [routed Rust run](https://github.com/EffortlessMetrics/shipper-swarm/actions/runs/30841375153) | success, including the required `Shipper Rust Small Result` |
| Clean exact-SHA local checks | `cargo fmt --all -- --check`; all 136 `shipper-webhook` tests; the `shipper-config` TOML round-trip property test; advisory doc contracts with `documents=22`, `findings=0`, `blocking=0`; `git diff --check` |
| [webhook security PR #236](https://github.com/EffortlessMetrics/shipper-swarm/pull/236) | merged into the candidate; GitGuardian, ripr, required Rust, and focused secret-redaction checks passed on its reviewed head |
| [legacy resume PR #239](https://github.com/EffortlessMetrics/shipper-swarm/pull/239) | exact-head routed result, isolated package check, interruption upload, and download/resume rehearsal passed; real v0.4.0 release-asset replay passed locally with 13/13 packages already complete and zero uploads |
| [package-check isolation PR #241](https://github.com/EffortlessMetrics/shipper-swarm/pull/241) | exact-head hosted fallback and normalized routed result passed after isolating Cargo's temporary package registry |
| [prior promotion PR #467](https://github.com/EffortlessMetrics/shipper/pull/467) | historical candidate promotion merged non-fast-forward as `c0711f60f0be5a4f88fe13d004af2d1bc73dcc22`; no tag, publication, deployment, or credential movement |
| [historical promotion PR #469](https://github.com/EffortlessMetrics/shipper/pull/469) | merged non-fast-forward as `978d0cd6b598f8967ac7f9adf358192362762e9c`; required routed result and hosted fallback passed, while queued auxiliary checks remain unavailable evidence |
| [historical promotion routed run](https://github.com/EffortlessMetrics/shipper/actions/runs/30860271124) | hosted fallback and normalized `Shipper Rust Small Result` passed on the sync head |
| Post-merge release-authority checks | `cargo fmt --all -- --check`, the legacy resume consistency regression, and advisory doc contracts passed locally |
| `shipper plan --format json` | deterministic plan `e722888e4182abe80891e770001636c9915c09e94c8ac8a479089757dbf854d1`; 13 publishable crates across 7 dependency levels |
| Topological `cargo publish --dry-run --locked` | dependency-free crates 1 through 7 passed; the first dependent crate, `shipper-types`, correctly stopped because `shipper-duration 0.5.0` is not yet present in crates.io |
| Topological `cargo package --locked` | the prior dependency-level probe recorded the pre-publication registry-resolution gap; the complete workspace package gate passed on PR #239 and PR #469 after package-registry isolation |
| Per-package semver checks against 0.4.0 | `cargo semver-checks check-release -p <publishable-crate> --baseline-version 0.4.0` passed for all 13 publishable crates: `shipper-cargo-failure`, `shipper-duration`, `shipper-encrypt`, `shipper-output-sanitizer`, `shipper-retry`, `shipper-sparse-index`, `shipper-webhook`, `shipper-types`, `shipper-config`, `shipper-registry`, `shipper-core`, `shipper-cli`, and `shipper` |
| Historical release binary matrix | [PR #473](https://github.com/EffortlessMetrics/shipper/pull/473) merged the runner correction as `8d80ff0d8301578fc8d71291076809dd46c8534d`; [workflow run 30867482676](https://github.com/EffortlessMetrics/shipper/actions/runs/30867482676) passed on that exact SHA and uploaded Linux x64, Intel macOS, arm64 macOS, and Windows x64 artifacts. The live artifact metadata checked on 2026-09-08 UTC reports all four expired on 2026-08-11. They are unavailable for current candidate proof. Publish, rehearse, resume, policy, and GitHub Release jobs were skipped. |

The following final publication proof remains intentionally open:
topological publish dry-run, release rehearsal, and publication-train recovery.
Real 0.4.0 release artifacts have been loaded
and resumed successfully by both the 0.4.0 binary and that historical candidate.
No tag, crate publication, deployment, or release credential movement has
occurred.

## Historical promotion boundary

The earlier frozen swarm SHA was merged into
`EffortlessMetrics/shipper/main` with the non-fast-forward merge recorded
above, and swarm was backfilled to that release merge at the time. Later
development superseded that publication identity. A new candidate and promotion
must follow the current preparation control record; this historical relationship
does not authorize a tag, crate publication, deployment, or release-credential
use.
