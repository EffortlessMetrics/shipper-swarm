# Contributing: spec rails

Use `.shipper-spec/` for durable proposal/spec/ADR/lane/closeout artifacts.

Do not place durable rails in `.codex/`, `.spec/`, `.claude/`, or `.jules/`.

When adding or changing artifacts, update `.shipper-spec/index.toml` so links remain resolvable.
